version: '3.8'

services:
  survey-kiosk:
    image: ghcr.io/zuptalo/survey-kiosk:react-latest
    container_name: survey-kiosk
    expose:
      - "3001"
    volumes:
      # Persist survey data and uploaded images
      - /opt/tools/survey-kiosk:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3001
      - APP_NAME=${APP_NAME:-Zuptalo}
      - ADMIN_PASSWORD=change-this-to-your-desired-password
      - SESSION_SECRET=change-this-to-a-random-secret-in-production
      - THANK_YOU_COUNTDOWN=5
      - INACTIVITY_TIMEOUT=30
    restart: unless-stopped
    networks:
      - prod-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

networks:
  prod-network:
    external: true
